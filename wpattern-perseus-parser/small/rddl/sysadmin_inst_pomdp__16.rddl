non-fluents nf_sysadmin_inst_pomdp__16 {
	domain = sysadmin_pomdp;
	objects {
		computer : {c1,c2,c3,c4};
	};
	non-fluents {
		REBOOT-PROB = 0.05;
		CONNECTED(c1,c3);
		CONNECTED(c2,c3);
		CONNECTED(c4,c1);
	};
}

instance sysadmin_inst_pomdp__16 {
	domain = sysadmin_pomdp;
	non-fluents = nf_sysadmin_inst_pomdp__16;
	init-state {
		running(c1);
		running(c2);
		running(c3);
		running(c4);
	};

	max-nondef-actions = 1;
	horizon  = 30;
	discount = 1.0;
}
